RSpec.describe Inter::Galaxy::Forex::Converter do
  it "has a version number" do
    expect(Inter::Galaxy::Forex::Converter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
